import { auth, SignedIn, SignedOut, UserButton } from "@clerk/nextjs";

export default function Home() {
    return (
        <div className="w-full border-4 border-red-500 h-[200px]">
            Home
        </div>
    );
}
